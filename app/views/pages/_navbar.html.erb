<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">User Manager</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
      <li class="nav-item">
      <%=link_to "All User", users_path, class: "nav-link"  %> 
      </li>        
        <% if !user_signed_in? %>

            <li class="nav-item">
                <%=link_to "Sign Up", new_user_registration_path, class: "nav-link"  %>
                
            </li>
            <li class="nav-item">
            <%=link_to "New User", new_user_session_path, class: "nav-link"  %>
            
        </li>
            <li class="nav-item">
                <%=link_to "Sign In", user_session_path, class: "nav-link"  %>
                </li>
        <% else %>
            
            <li class="nav-item">
            <%=link_to "Edit User", edit_user_registration_path, class: "nav-link"  %> 
            </li>        
        
        <%end%>
    
        
      </ul>
    </div>
  </div>
  <div class="ml-auto"> <!-- This div aligns content to the right -->
  <% if user_signed_in? %>
    <%= form_tag(destroy_user_session_path, method: :delete) do %>
        <%= submit_tag "Sign out", class: "btn btn-danger" %>
    <% end %>
  <% end %>

</div>
</nav>